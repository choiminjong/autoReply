# Phase 1.5d 테스트 체크리스트

> Build Date: 2026-03-25
> Scope: 관리자 API, 관리자 UI, 삭제 캐스케이드, UI 통합

---

## 1. 관리자 API

- [ ] `GET /api/admin/users` — admin 역할만 접근 가능한가? (user로 접근 시 403)
- [ ] 사용자 목록에 Outlook 연동 이메일 및 연동 일시가 포함되는가?
- [ ] `PATCH /api/admin/users/{id}` — 역할 변경 (user → admin) 이 반영되는가?
- [ ] 자기 자신의 역할을 변경하려 할 때 400이 반환되는가?
- [ ] 마지막 admin을 user로 변경하려 할 때 400이 반환되는가?
- [ ] 사용자 비활성화 시 해당 사용자의 세션이 삭제되는가?
- [ ] 사용자 비활성화 시 해당 사용자의 클레임이 모두 해제되는가?
- [ ] `GET /api/admin/settings` — 시스템 설정 전체 조회 가능한가?
- [ ] `PATCH /api/admin/settings/{key}` — 설정 변경이 DB에 반영되는가?
- [ ] 허용되지 않은 key 변경 시 400이 반환되는가?

---

## 2. 삭제 캐스케이드 정책

- [ ] 사용자 비활성화 → 세션 삭제 + 클레임 해제 + 데이터(쓰레드/댓글)는 유지
- [ ] 프로젝트 삭제 → `project_members` 삭제, `threads.project_id = NULL`, `comments.project_id = NULL`
- [ ] Outlook 연동 해제 → `outlook_tokens` 삭제, 기존 동기화 데이터는 유지
- [ ] 멤버 제거 → 해당 프로젝트에서 그 사람의 클레임만 해제, 댓글은 유지

---

## 3. 관리자 페이지 UI (`/admin`)

### 사용자 관리 탭
- [ ] admin 역할이 아닌 사용자가 `/admin` 접근 시 `/`로 리다이렉트되는가?
- [ ] 사용자 목록이 테이블로 표시되는가? (이름, 이메일, 역할, Outlook, 상태)
- [ ] 역할 드롭다운 변경 → 확인 팝업 → API 호출 → 페이지 갱신이 되는가?
- [ ] 자기 자신의 행은 역할 드롭다운 대신 배지만 표시되는가?
- [ ] 비활성화 버튼 → 확인 팝업 → 비활성화 반영이 되는가?
- [ ] 비활성화된 사용자는 활성화 버튼이 표시되는가?

### 시스템 설정 탭
- [ ] 기본 세션 유효 시간 선택 + 저장이 동작하는가?
- [ ] 최대 세션 유효 시간 선택 + 저장이 동작하는가?
- [ ] 미클레임 알림 기준 저장이 동작하는가?
- [ ] 팀 Slack Webhook URL 입력 + 저장이 동작하는가?
- [ ] 페이지 진입 시 현재 설정값이 폼에 자동으로 채워지는가?

### 프로젝트 현황 탭
- [ ] 전체 프로젝트 목록 (이름, 메일링리스트, 팀원 수, 미클레임) 이 표시되는가?

---

## 4. UI 통합 — 헤더 네비게이션

### 칸반 보드 (`/`)
- [ ] 헤더에 "📁 프로젝트 전환", "⚙️ 설정", "🛡 관리"(admin), "로그아웃" 링크가 표시되는가?
- [ ] 프로젝트가 선택된 경우 헤더에 현재 프로젝트 이름이 표시되는가?
- [ ] 프로젝트 이름 클릭 시 `/projects`로 이동하는가?
- [ ] admin이 아닌 경우 "관리" 링크가 숨겨지는가?

### 페이지 간 이동
- [ ] `/login` → 로그인 → `/` (프로젝트 미선택 시 전체 뷰) 로 이동하는가?
- [ ] `/projects` → 프로젝트 카드 클릭 → `/` (프로젝트 필터 적용) 로 이동하는가?
- [ ] `/settings` → 뒤로가기 링크 → `/` 로 이동하는가?
- [ ] `/admin` → 뒤로가기 링크 → `/` 로 이동하는가?

---

## 공통

- [ ] 서버 시작 시 모든 마이그레이션(001, 002)이 오류 없이 실행되는가?
- [ ] 모든 신규 페이지가 미인증 시 `/login`으로 리다이렉트되는가?
- [ ] 모든 상태 변경 요청에 CSRF 토큰이 정상 첨부되는가?
